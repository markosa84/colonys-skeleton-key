# Use the locally installed JDK 25 for the toolchain; never fetch one over the network.
# The Gradle daemon already runs on Corretto 25, so the "current JVM" satisfies the toolchain.
org.gradle.java.installations.auto-download=false
